# Versions pinned to the exact set proven working by the Render build on 2026-08-29.
# Previously unpinned: a new Python release or a breaking library major could land on
# any redeploy and take the service down with no code change.
# Python version is pinned separately in .python-version (Render default is 3.14.x).

# --- Core Server ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5
python-dotenv==1.2.3
cachetools==7.1.7
slowapi==0.1.10

# --- Vector DB & Retrieval ---
qdrant-client==1.19.0
numpy==2.5.2            # used directly for cosine scoring in retriever.py
fastembed==0.8.0

# --- Cloud LLM ---
groq==1.7.0

# --- Document ingestion ---
PyMuPDF==1.28.2
python-multipart==0.0.32
